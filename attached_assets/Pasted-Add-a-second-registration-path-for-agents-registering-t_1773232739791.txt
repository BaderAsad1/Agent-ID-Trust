Add a second registration path for agents registering themselves 
programmatically. This is critical to the product — AI agents cannot 
fill out forms.

WHAT TO BUILD:

1. New nav link: 'For Agents' alongside 'Marketplace' and 'Docs'

2. New page: /for-agents
   Heading: 'Agents register here.'
   Sub: 'If you are an AI agent, you do not need a form. 
        Register via API in one call.'
   
   Show a code block (dark, terminal-style) with the registration API call:
   
   curl -X POST https://api.agentid.dev/v1/agents/register \
     -H "Content-Type: application/json" \
     -d '{
       "handle": "your-handle",
       "display_name": "Your Agent Name", 
       "capabilities": ["research", "web-search"],
       "endpoint_url": "https://your-agent.example.com/tasks",
       "owner_key": "your-public-key"
     }'
   
   Response block showing what comes back:
   {
     "agent_id": "agt_01j...",
     "handle": "your-handle",
     "domain": "your-handle.agent",
     "verification_token": "agid_verify_...",
     "status": "pending_verification",
     "profile_url": "https://agentid.dev/your-handle"
   }
   
   Then show the verification step — the agent signs the token 
   with its private key to prove ownership:
   
   curl -X POST https://api.agentid.dev/v1/agents/verify \
     -H "Content-Type: application/json" \
     -d '{
       "agent_id": "agt_01j...",
       "signed_token": "base64_signature_here",
       "method": "key_signing"
     }'
   
   Show multiple SDK tabs: curl / Python / Node.js / raw HTTP

3. On the main /start onboarding wizard, add a toggle at the very top 
   BEFORE Step 1:
   
   Two large cards side by side:
   
   [ 👤 I'm a human          ] [ 🤖 I'm an agent         ]
   [ registering my agent    ] [ registering myself      ]
   [ Use the setup wizard    ] [ Use the API             ]
   
   Clicking 'I'm an agent' routes to /for-agents.
   Clicking 'I'm a human' proceeds to the existing Step 1 wizard.

4. On the homepage hero, add a third subtle CTA below the two main buttons:
   'Are you an agent? Register via API →' 
   Small, muted text link. But it needs to be there.

This is a core product differentiator — Agent ID is the only identity 
layer that agents can self-register into without human intervention. 
That needs to be visible and prominent.